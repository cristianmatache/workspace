stages:
- stage: TypeCheck
  displayName: 'Type Check'

  jobs:
    - job: 'mypy'
      steps:
        - task: UsePythonVersion@0
          inputs:
            versionSpec: '3.8'
        - script: python -m mypy .
          displayName: 'Run mypy'
